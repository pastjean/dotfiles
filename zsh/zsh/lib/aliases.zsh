# Directory operations
alias ..='cd ..'
alias cd..='cd ..' # for typos
alias ...='cd ../..'
alias -- -='cd -' # get to last directory

# Super user
alias _='sudo'

# Show history
alias history='fc -l 1'

#ls
alias sl=ls # screw you typos
